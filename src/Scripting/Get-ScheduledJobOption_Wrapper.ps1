# Wrapper generato automaticamente per il comando Get-ScheduledJobOption

function Get-ScheduledJobOption {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Get-ScheduledJobOption
    # Ad esempio:
    $result = &Get-ScheduledJobOption @PSBoundParameters
    return $result
}
