# Wrapper generato automaticamente per il comando Set-ScheduledJob

function Set-ScheduledJob {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Set-ScheduledJob
    # Ad esempio:
    $result = &Set-ScheduledJob @PSBoundParameters
    return $result
}
