# Wrapper generato automaticamente per il comando C:

function C: {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando C:
    # Ad esempio:
    $result = &C: @PSBoundParameters
    return $result
}
