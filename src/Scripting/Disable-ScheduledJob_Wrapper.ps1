# Wrapper generato automaticamente per il comando Disable-ScheduledJob

function Disable-ScheduledJob {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Disable-ScheduledJob
    # Ad esempio:
    $result = &Disable-ScheduledJob @PSBoundParameters
    return $result
}
