# Wrapper generato automaticamente per il comando PSConsoleHostReadLine

function PSConsoleHostReadLine {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando PSConsoleHostReadLine
    # Ad esempio:
    $result = &PSConsoleHostReadLine @PSBoundParameters
    return $result
}
