# Wrapper generato automaticamente per il comando Remove-JobTrigger

function Remove-JobTrigger {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Remove-JobTrigger
    # Ad esempio:
    $result = &Remove-JobTrigger @PSBoundParameters
    return $result
}
