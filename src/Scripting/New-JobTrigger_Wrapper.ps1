# Wrapper generato automaticamente per il comando New-JobTrigger

function New-JobTrigger {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando New-JobTrigger
    # Ad esempio:
    $result = &New-JobTrigger @PSBoundParameters
    return $result
}
