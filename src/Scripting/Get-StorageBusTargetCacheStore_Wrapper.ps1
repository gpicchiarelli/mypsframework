# Wrapper generato automaticamente per il comando Get-StorageBusTargetCacheStore

function Get-StorageBusTargetCacheStore {
    param (
        [SwitchParameter] $AsJob
    )

    # Inserisci qui la logica per utilizzare il comando Get-StorageBusTargetCacheStore
    # Ad esempio:
    $result = &Get-StorageBusTargetCacheStore @PSBoundParameters
    return $result
}
