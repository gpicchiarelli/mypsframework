# Wrapper generato automaticamente per il comando __VSCode-Escape-Value

function __VSCode-Escape-Value {
    param (
        [String] $value
    )

    # Inserisci qui la logica per utilizzare il comando __VSCode-Escape-Value
    # Ad esempio:
    $result = &__VSCode-Escape-Value @PSBoundParameters
    return $result
}
