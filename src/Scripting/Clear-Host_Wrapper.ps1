# Wrapper generato automaticamente per il comando Clear-Host

function Clear-Host {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando Clear-Host
    # Ad esempio:
    $result = &Clear-Host @PSBoundParameters
    return $result
}
