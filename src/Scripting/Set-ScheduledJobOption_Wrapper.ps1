# Wrapper generato automaticamente per il comando Set-ScheduledJobOption

function Set-ScheduledJobOption {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Set-ScheduledJobOption
    # Ad esempio:
    $result = &Set-ScheduledJobOption @PSBoundParameters
    return $result
}
