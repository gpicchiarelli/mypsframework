# Wrapper generato automaticamente per il comando New-ScheduledJobOption

function New-ScheduledJobOption {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando New-ScheduledJobOption
    # Ad esempio:
    $result = &New-ScheduledJobOption @PSBoundParameters
    return $result
}
