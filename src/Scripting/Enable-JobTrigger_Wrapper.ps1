# Wrapper generato automaticamente per il comando Enable-JobTrigger

function Enable-JobTrigger {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Enable-JobTrigger
    # Ad esempio:
    $result = &Enable-JobTrigger @PSBoundParameters
    return $result
}
