# Wrapper generato automaticamente per il comando Disable-PSTrace

function Disable-PSTrace {
    param (
        [SwitchParameter] $AnalyticOnly
    )

    # Inserisci qui la logica per utilizzare il comando Disable-PSTrace
    # Ad esempio:
    $result = &Disable-PSTrace @PSBoundParameters
    return $result
}
