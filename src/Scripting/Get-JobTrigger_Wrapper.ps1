# Wrapper generato automaticamente per il comando Get-JobTrigger

function Get-JobTrigger {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Get-JobTrigger
    # Ad esempio:
    $result = &Get-JobTrigger @PSBoundParameters
    return $result
}
