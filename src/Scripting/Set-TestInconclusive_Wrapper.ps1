# Wrapper generato automaticamente per il comando Set-TestInconclusive

function Set-TestInconclusive {
    param (
        [String] $Message
    )

    # Inserisci qui la logica per utilizzare il comando Set-TestInconclusive
    # Ad esempio:
    $result = &Set-TestInconclusive @PSBoundParameters
    return $result
}
