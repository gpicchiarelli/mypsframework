# Wrapper generato automaticamente per il comando Set-JobTrigger

function Set-JobTrigger {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Set-JobTrigger
    # Ad esempio:
    $result = &Set-JobTrigger @PSBoundParameters
    return $result
}
