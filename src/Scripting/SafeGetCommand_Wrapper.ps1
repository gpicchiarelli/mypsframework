# Wrapper generato automaticamente per il comando SafeGetCommand

function SafeGetCommand {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando SafeGetCommand
    # Ad esempio:
    $result = &SafeGetCommand @PSBoundParameters
    return $result
}
