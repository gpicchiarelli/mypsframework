# Wrapper generato automaticamente per il comando Enable-ScheduledJob

function Enable-ScheduledJob {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Enable-ScheduledJob
    # Ad esempio:
    $result = &Enable-ScheduledJob @PSBoundParameters
    return $result
}
