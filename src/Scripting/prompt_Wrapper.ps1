# Wrapper generato automaticamente per il comando prompt

function prompt {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando prompt
    # Ad esempio:
    $result = &prompt @PSBoundParameters
    return $result
}
