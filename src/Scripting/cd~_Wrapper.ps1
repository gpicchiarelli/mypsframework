# Wrapper generato automaticamente per il comando cd~

function cd~ {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando cd~
    # Ad esempio:
    $result = &cd~ @PSBoundParameters
    return $result
}
