# Wrapper generato automaticamente per il comando Get-TerminalIconsIconTheme

function Get-TerminalIconsIconTheme {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando Get-TerminalIconsIconTheme
    # Ad esempio:
    $result = &Get-TerminalIconsIconTheme @PSBoundParameters
    return $result
}
