# Wrapper generato automaticamente per il comando Register-ScheduledJob

function Register-ScheduledJob {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Register-ScheduledJob
    # Ad esempio:
    $result = &Register-ScheduledJob @PSBoundParameters
    return $result
}
