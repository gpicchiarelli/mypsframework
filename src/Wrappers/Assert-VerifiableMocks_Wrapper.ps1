# Wrapper generato automaticamente per il comando Assert-VerifiableMocks

function Assert-VerifiableMocks {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando Assert-VerifiableMocks
    # Ad esempio:
    $result = &Assert-VerifiableMocks @PSBoundParameters
    return $result
}
