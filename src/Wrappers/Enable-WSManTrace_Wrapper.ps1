# Wrapper generato automaticamente per il comando Enable-WSManTrace

function Enable-WSManTrace {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando Enable-WSManTrace
    # Ad esempio:
    $result = &Enable-WSManTrace @PSBoundParameters
    return $result
}
