# Wrapper generato automaticamente per il comando Get-DOPercentageMaxBackgroundBandwidth

function Get-DOPercentageMaxBackgroundBandwidth {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Get-DOPercentageMaxBackgroundBandwidth
    # Ad esempio:
    $result = &Get-DOPercentageMaxBackgroundBandwidth @PSBoundParameters
    return $result
}
