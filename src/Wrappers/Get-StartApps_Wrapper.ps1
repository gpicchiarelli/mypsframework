# Wrapper generato automaticamente per il comando Get-StartApps

function Get-StartApps {
    param (
        [Object] $Name
    )

    # Inserisci qui la logica per utilizzare il comando Get-StartApps
    # Ad esempio:
    $result = &Get-StartApps @PSBoundParameters
    return $result
}
