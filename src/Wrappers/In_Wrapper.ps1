# Wrapper generato automaticamente per il comando In

function In {
    param (
        [Object] $path,
        [ScriptBlock] $execute
    )

    # Inserisci qui la logica per utilizzare il comando In
    # Ad esempio:
    $result = &In @PSBoundParameters
    return $result
}
