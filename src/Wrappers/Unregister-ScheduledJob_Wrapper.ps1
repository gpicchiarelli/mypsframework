# Wrapper generato automaticamente per il comando Unregister-ScheduledJob

function Unregister-ScheduledJob {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Unregister-ScheduledJob
    # Ad esempio:
    $result = &Unregister-ScheduledJob @PSBoundParameters
    return $result
}
