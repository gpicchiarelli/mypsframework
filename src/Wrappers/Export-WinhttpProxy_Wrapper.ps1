# Wrapper generato automaticamente per il comando Export-WinhttpProxy

function Export-WinhttpProxy {
    param (
        [SwitchParameter] $AsJob
    )

    # Inserisci qui la logica per utilizzare il comando Export-WinhttpProxy
    # Ad esempio:
    $result = &Export-WinhttpProxy @PSBoundParameters
    return $result
}
