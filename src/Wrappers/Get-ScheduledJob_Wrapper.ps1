# Wrapper generato automaticamente per il comando Get-ScheduledJob

function Get-ScheduledJob {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Get-ScheduledJob
    # Ad esempio:
    $result = &Get-ScheduledJob @PSBoundParameters
    return $result
}
