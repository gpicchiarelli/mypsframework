# Wrapper generato automaticamente per il comando Enable-PSTrace

function Enable-PSTrace {
    param (
        [SwitchParameter] $Force,
        [SwitchParameter] $AnalyticOnly
    )

    # Inserisci qui la logica per utilizzare il comando Enable-PSTrace
    # Ad esempio:
    $result = &Enable-PSTrace @PSBoundParameters
    return $result
}
