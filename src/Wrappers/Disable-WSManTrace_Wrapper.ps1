# Wrapper generato automaticamente per il comando Disable-WSManTrace

function Disable-WSManTrace {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando Disable-WSManTrace
    # Ad esempio:
    $result = &Disable-WSManTrace @PSBoundParameters
    return $result
}
