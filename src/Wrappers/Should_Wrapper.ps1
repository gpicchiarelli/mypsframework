# Wrapper generato automaticamente per il comando Should

function Should {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando Should
    # Ad esempio:
    $result = &Should @PSBoundParameters
    return $result
}
