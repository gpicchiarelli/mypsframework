# Wrapper generato automaticamente per il comando AfterAll

function AfterAll {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando AfterAll
    # Ad esempio:
    $result = &AfterAll @PSBoundParameters
    return $result
}
