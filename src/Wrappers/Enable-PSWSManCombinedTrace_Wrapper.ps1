# Wrapper generato automaticamente per il comando Enable-PSWSManCombinedTrace

function Enable-PSWSManCombinedTrace {
    param (
        [SwitchParameter] $DoNotOverwriteExistingTrace
    )

    # Inserisci qui la logica per utilizzare il comando Enable-PSWSManCombinedTrace
    # Ad esempio:
    $result = &Enable-PSWSManCombinedTrace @PSBoundParameters
    return $result
}
