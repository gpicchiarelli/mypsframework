# Wrapper generato automaticamente per il comando Get-TerminalIconsColorTheme

function Get-TerminalIconsColorTheme {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando Get-TerminalIconsColorTheme
    # Ad esempio:
    $result = &Get-TerminalIconsColorTheme @PSBoundParameters
    return $result
}
