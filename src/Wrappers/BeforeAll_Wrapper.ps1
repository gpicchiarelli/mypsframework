# Wrapper generato automaticamente per il comando BeforeAll

function BeforeAll {
    param (
        
    )

    # Inserisci qui la logica per utilizzare il comando BeforeAll
    # Ad esempio:
    $result = &BeforeAll @PSBoundParameters
    return $result
}
