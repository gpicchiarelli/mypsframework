# Wrapper generato automaticamente per il comando Flush-EtwTraceSession

function Flush-EtwTraceSession {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Flush-EtwTraceSession
    # Ad esempio:
    $result = &Flush-EtwTraceSession @PSBoundParameters
    return $result
}
