# Wrapper generato automaticamente per il comando Disable-JobTrigger

function Disable-JobTrigger {
    param (
        [] $
    )

    # Inserisci qui la logica per utilizzare il comando Disable-JobTrigger
    # Ad esempio:
    $result = &Disable-JobTrigger @PSBoundParameters
    return $result
}
