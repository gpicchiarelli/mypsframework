# Wrapper generato automaticamente per il comando Get-TestDriveItem

function Get-TestDriveItem {
    param (
        [String] $Path
    )

    # Inserisci qui la logica per utilizzare il comando Get-TestDriveItem
    # Ad esempio:
    $result = &Get-TestDriveItem @PSBoundParameters
    return $result
}
